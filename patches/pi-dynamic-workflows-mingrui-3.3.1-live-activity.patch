diff --git a/dist/agent.d.ts b/dist/agent.d.ts
index a85f9751f59922df5ab0699f407282551430c204..ecd80c3d99129ef1583be543aa5b01994c0d377a 100644
--- a/dist/agent.d.ts
+++ b/dist/agent.d.ts
@@ -102,6 +102,12 @@ export interface WorkflowAgentOptions {
      * Default: false (current behavior).
      */
     persistAgentSessions?: boolean;
+    /** Optional timestamped stage probe for isolated diagnostics/tests. */
+    onDiagnostic?: (event: {
+        stage: string;
+        at: number;
+        label?: string;
+    }) => void;
 }
 /**
  * The ModelRuntime behind a registry facade. pi's ModelRegistry does not expose
@@ -258,6 +264,7 @@ export declare class WorkflowAgent {
     private readonly mainModel?;
     /** Shared registry from the host session, when provided. */
     private readonly sharedRegistry?;
+    private readonly onDiagnostic?;
     /** Lazily built once; shares the SDK's agentDir/auth so resolved models are authed. */
     private registry?;
     /**
@@ -272,6 +279,7 @@ export declare class WorkflowAgent {
      */
     private sharedResourceLoaderPromise?;
     constructor(options?: WorkflowAgentOptions);
+    private diagnostic;
     /**
      * A resource loader shared by every subagent of this run, built once (#109).
      *
diff --git a/dist/agent.js b/dist/agent.js
index 3b0f2a36a048946d0ea1a3f6abf2c90a62876c36..0821a6eb7a6df563b4a2abe791e68fa23039b6b6 100644
--- a/dist/agent.js
+++ b/dist/agent.js
@@ -326,6 +326,7 @@ export class WorkflowAgent {
     mainModel;
     /** Shared registry from the host session, when provided. */
     sharedRegistry;
+    onDiagnostic;
     /** Lazily built once; shares the SDK's agentDir/auth so resolved models are authed. */
     registry;
     /**
@@ -348,6 +349,10 @@ export class WorkflowAgent {
         this.instructions = options.instructions;
         this.mainModel = options.mainModel;
         this.sharedRegistry = options.modelRegistry;
+        this.onDiagnostic = options.onDiagnostic;
+    }
+    diagnostic(stage, label) {
+        this.onDiagnostic?.({ stage, at: Date.now(), label });
     }
     /**
      * A resource loader shared by every subagent of this run, built once (#109).
@@ -504,7 +509,9 @@ export class WorkflowAgent {
         // Per-run modelRegistry wins over the constructor's shared registry, then
         // the lazily-built disk fallback. Used for tier diagnostics, model
         // resolution, and the subagent session's runtime below.
+        this.diagnostic("registry:start", options.label);
         const modelRegistry = await this.getRegistry(options.modelRegistry);
+        this.diagnostic("registry:end", options.label);
         // Resolve the model spec (explicit model > tier > session default). This
         // composes with phase-based routing in workflow.ts, which only supplies
         // options.model when a phase pattern matches — so an explicit model wins.
@@ -538,6 +545,10 @@ export class WorkflowAgent {
         // group under the project's session dir instead of scattering across
         // temporary worktree paths.
         const sessionManager = this.createSessionManager();
+        this.diagnostic("resource_loader:start", options.label);
+        const resourceLoader = this.sessionOptions.resourceLoader ?? (await this.getSharedResourceLoader(agentDir));
+        this.diagnostic("resource_loader:end", options.label);
+        this.diagnostic("create_session:start", options.label);
         const { session } = await createAgentSession({
             cwd: runCwd,
             agentDir,
@@ -552,7 +563,7 @@ export class WorkflowAgent {
             // getSharedResourceLoader. An injected resourceLoader (tests / embedders)
             // wins and skips the shared build entirely; the ...this.sessionOptions
             // spread below re-applies the same injected value harmlessly.
-            resourceLoader: this.sessionOptions.resourceLoader ?? (await this.getSharedResourceLoader(agentDir)),
+            resourceLoader,
             // Share the resolved registry's ModelRuntime (catalog + auth, including
             // extension-registered providers) with the subagent session. pi >= 0.80.8
             // takes modelRuntime here; the old modelRegistry option is gone.
@@ -566,6 +577,7 @@ export class WorkflowAgent {
             // the caller set on sessionOptions rather than dropping it.
             excludeTools: subagentExcludedTools(this.excludeTools, this.sessionOptions.excludeTools),
         });
+        this.diagnostic("create_session:end", options.label);
         // Name the persisted session so it's identifiable in session pickers.
         // Skip when an injected session.sessionManager override won (tests/embedders).
         if (this.persistAgentSessions && !this.sessionOptions.sessionManager && options.sessionName) {
@@ -579,7 +591,16 @@ export class WorkflowAgent {
         let removeAbortListener;
         let removeHistoryListener;
         let lastHistoryEmit = 0;
-        const emitHistory = () => options.onHistory?.(compactAgentHistory(session.messages));
+        let sawSessionEvent = false;
+        let sawHistory = false;
+        const emitHistory = () => {
+            const history = compactAgentHistory(session.messages);
+            if (!sawHistory && history.length > 0) {
+                sawHistory = true;
+                this.diagnostic("history:first_nonempty", options.label);
+            }
+            options.onHistory?.(history);
+        };
         const maybeEmitHistory = () => {
             if (!options.onHistory)
                 return;
@@ -598,9 +619,17 @@ export class WorkflowAgent {
                 removeAbortListener = () => options.signal?.removeEventListener("abort", onAbort);
             }
             if (options.onHistory) {
-                removeHistoryListener = session.subscribe(() => maybeEmitHistory());
+                removeHistoryListener = session.subscribe(() => {
+                    if (!sawSessionEvent) {
+                        sawSessionEvent = true;
+                        this.diagnostic("session_event:first", options.label);
+                    }
+                    maybeEmitHistory();
+                });
             }
+            this.diagnostic("prompt:before", options.label);
             await session.prompt(this.buildPrompt(prompt, options, Boolean(options.schema)));
+            this.diagnostic("prompt:after", options.label);
             if (options.signal?.aborted)
                 throw new Error("Subagent was aborted");
             // The SDK buries a provider usage/quota limit in the assistant message rather
diff --git a/dist/display.d.ts b/dist/display.d.ts
index 2677233ec7537f57c3930e41e08318694b0fb234..78a489675129b9a7deb43d0fb027516fc2e849b0 100644
--- a/dist/display.d.ts
+++ b/dist/display.d.ts
@@ -19,8 +19,14 @@ export interface WorkflowAgentSnapshot {
     tokens?: number;
     /** Per-agent token usage breakdown (fresh input+output vs cached), when known. */
     tokenUsage?: AgentUsage;
-    /** The model this agent ran on (provider/id), when known. */
+    /** The concrete model this agent ran on (provider/id), once runtime resolution completes. */
     model?: string;
+    /** Requested explicit/phase model, retained for diagnostics while resolution is pending. */
+    requestedModel?: string;
+    /** Requested model tier, retained for diagnostics while resolution is pending. */
+    requestedTier?: string;
+    /** The requested model could not resolve; the session default was used. */
+    modelFallback?: boolean;
 }
 export interface WorkflowSnapshot {
     name: string;
diff --git a/dist/run-persistence.d.ts b/dist/run-persistence.d.ts
index dfe9aff920b09b2cc9e8e791743d5baf9da4b0f0..c7879cf952f09540a5fb7da52995c981be379753 100644
--- a/dist/run-persistence.d.ts
+++ b/dist/run-persistence.d.ts
@@ -23,8 +23,14 @@ export interface PersistedAgentState {
     tokens?: number;
     /** Per-agent token usage breakdown, when the provider reported one. */
     tokenUsage?: AgentUsage;
-    /** The model this agent ran on (provider/id), when known. */
+    /** The concrete model this agent ran on (provider/id), once runtime resolution completes. */
     model?: string;
+    /** Requested explicit/phase model, retained for diagnostics while resolution is pending. */
+    requestedModel?: string;
+    /** Requested model tier, retained for diagnostics while resolution is pending. */
+    requestedTier?: string;
+    /** The requested model could not resolve; the session default was used. */
+    modelFallback?: boolean;
 }
 export interface PersistedRunState {
     runId: string;
diff --git a/dist/task-panel.js b/dist/task-panel.js
index 50c3da380e60a9e171e4fc38b230201dad5a018b..6bf102b2907a41b3e9a0a959007d94bedc882d82 100644
--- a/dist/task-panel.js
+++ b/dist/task-panel.js
@@ -14,6 +14,8 @@ import { shortModel } from "./workflow-ui.js";
 // it redraws identical content.
 const RUN_EVENTS = [
     "agentStart",
+    "agentHistory",
+    "agentHeartbeat",
     "agentEnd",
     "phase",
     "log",
diff --git a/dist/workflow-manager.d.ts b/dist/workflow-manager.d.ts
index e2284e8593570215491e78e8388a275c475cc94b..b4d72d44ab0450645511688b583cf5ba3fd9fbad 100644
--- a/dist/workflow-manager.d.ts
+++ b/dist/workflow-manager.d.ts
@@ -219,6 +219,17 @@ export interface WorkflowManagerOptions {
      * to observe eviction without creating dozens of runs.
      */
     maxTerminalRunsInMemory?: number;
+    /** Interval for a run-level liveness heartbeat while at least one agent is silent/running. */
+    heartbeatIntervalMs?: number;
+    /** Trailing-edge persistence window for non-critical progress updates. */
+    progressPersistThrottleMs?: number;
+    /** Optional timestamped stage probe for isolated diagnostics/tests. */
+    onDiagnostic?: (event: {
+        stage: string;
+        at: number;
+        label?: string;
+        runId?: string;
+    }) => void;
 }
 export declare class WorkflowManager extends EventEmitter {
     /**
@@ -289,6 +300,9 @@ export declare class WorkflowManager extends EventEmitter {
     private toolsets?;
     private excludeSubagentTools?;
     private persistAgentSessions;
+    private heartbeatIntervalMs;
+    private progressPersistThrottleMs;
+    private onDiagnostic?;
     constructor(options?: WorkflowManagerOptions);
     /** Bind the manager to the current pi session, so new runs are tagged with it and
      * the navigator/task-panel show only this session's runs (set on session_start). */
@@ -380,10 +394,12 @@ export declare class WorkflowManager extends EventEmitter {
      */
     private accumulateTokenUsage;
     private releaseRunLease;
-    /** Trailing-edge throttle window for high-frequency progress persists (see schedulePersist). */
-    private static readonly PERSIST_THROTTLE_MS;
     /** Pending trailing-edge persist timers for high-frequency progress events, keyed by runId. */
     private persistTimers;
+    /** One bounded heartbeat per run, regardless of its agent concurrency. */
+    private heartbeatTimers;
+    private ensureHeartbeat;
+    private clearHeartbeat;
     /**
      * Coalesce rapid progress persists (currently: onAgentJournal, which fires
      * once per completed agent and can burst under concurrency) to at most one
diff --git a/dist/workflow-manager.js b/dist/workflow-manager.js
index a246783b6ea952c7e838b0163ad95d81f5d8fa0f..6a5de01b2b11c494b39454c6887ea51c9b890414 100644
--- a/dist/workflow-manager.js
+++ b/dist/workflow-manager.js
@@ -97,6 +97,9 @@ export class WorkflowManager extends EventEmitter {
     toolsets;
     excludeSubagentTools;
     persistAgentSessions;
+    heartbeatIntervalMs;
+    progressPersistThrottleMs;
+    onDiagnostic;
     constructor(options = {}) {
         super();
         this.cwd = options.cwd ?? process.cwd();
@@ -113,6 +116,9 @@ export class WorkflowManager extends EventEmitter {
         this.toolsets = options.toolsets;
         this.excludeSubagentTools = options.excludeSubagentTools;
         this.persistAgentSessions = options.persistAgentSessions ?? false;
+        this.heartbeatIntervalMs = Math.max(10, options.heartbeatIntervalMs ?? 15_000);
+        this.progressPersistThrottleMs = Math.max(0, options.progressPersistThrottleMs ?? 400);
+        this.onDiagnostic = options.onDiagnostic;
         this.maxTerminalRunsInMemory = options.maxTerminalRunsInMemory ?? DEFAULT_MAX_TERMINAL_RUNS_IN_MEMORY;
         this.persistence = createRunPersistence(this.cwd);
         this.recoverStaleRuns();
@@ -401,6 +407,7 @@ export class WorkflowManager extends EventEmitter {
                 // runWorkflow only applies this on the fresh-SharedRuntime branch, never
                 // overriding an inherited options.sharedRuntime from a nested workflow()).
                 initialTokenUsage,
+                onDiagnostic: this.onDiagnostic,
                 // Retried-attempt spend (see WorkflowRunOptions.onRetrySpend and A2):
                 // recordTokens() in workflow.ts already folded this into
                 // shared.spent/tokenUsage, but onAgentEnd never sees a retried
@@ -425,6 +432,7 @@ export class WorkflowManager extends EventEmitter {
                 },
                 onLog: (message) => {
                     managed.snapshot.logs.push(message);
+                    this.schedulePersist(managed);
                     this.emitLive(managed, "log", { runId: managed.runId, message });
                     progress();
                 },
@@ -433,10 +441,12 @@ export class WorkflowManager extends EventEmitter {
                     if (!managed.snapshot.phases.includes(title)) {
                         managed.snapshot.phases.push(title);
                     }
+                    this.schedulePersist(managed);
                     this.emitLive(managed, "phase", { runId: managed.runId, title });
                     progress();
                 },
                 onAgentStart: (event) => {
+                    const hadRunningAgent = managed.snapshot.agents.some((agent) => agent.status === "running");
                     const id = managed.snapshot.agents.length + 1;
                     const agentSnapshot = {
                         id,
@@ -444,7 +454,8 @@ export class WorkflowManager extends EventEmitter {
                         phase: event.phase,
                         prompt: event.prompt,
                         status: "running",
-                        model: event.model,
+                        requestedModel: event.requestedModel,
+                        requestedTier: event.requestedTier,
                     };
                     managed.snapshot.agents.push(agentSnapshot);
                     // Index by the call's unique id (never label — see agentsById's doc
@@ -454,10 +465,43 @@ export class WorkflowManager extends EventEmitter {
                     // Real per-agent start time, captured the moment the agent actually
                     // starts (not the run's startedAt) — see agentTimestamps.
                     managed.agentTimestamps.set(id, { startedAt: new Date().toISOString() });
+                    // The first start in a wave is lifecycle-critical and durable now.
+                    // Sibling starts in the same concurrent wave coalesce into one trailing
+                    // snapshot; this keeps every agent visible within the bounded throttle
+                    // without turning an N-agent fan-out into N full JSON writes.
+                    if (hadRunningAgent)
+                        this.schedulePersist(managed);
+                    else
+                        this.persistRun(managed);
+                    this.ensureHeartbeat(managed);
                     this.emitLive(managed, "agentStart", { runId: managed.runId, ...event });
                     progress();
                 },
+                onAgentModelResolved: (event) => {
+                    const agent = managed.agentsById.get(event.id);
+                    if (agent) {
+                        agent.model = event.model;
+                        agent.modelFallback = false;
+                        // Resolution happens before prompt execution. Persist it now so
+                        // external observers see the effective model while the agent runs.
+                        this.persistRun(managed);
+                    }
+                    this.emitLive(managed, "agentModelResolved", { runId: managed.runId, ...event });
+                    progress();
+                },
+                onAgentModelFallback: (event) => {
+                    const agent = managed.agentsById.get(event.id);
+                    if (agent) {
+                        agent.modelFallback = true;
+                        // Do not put the request or host default in `model`: neither is a
+                        // verified effective model at this point.
+                        this.persistRun(managed);
+                    }
+                    this.emitLive(managed, "agentModelFallback", { runId: managed.runId, ...event });
+                    progress();
+                },
                 onAgentEnd: (event) => {
+                    this.onDiagnostic?.({ stage: "manager:agent_end", at: Date.now(), label: event.label, runId: managed.runId });
                     const agent = managed.agentsById.get(event.id);
                     if (agent) {
                         agent.status = event.result === null ? "error" : "done";
@@ -491,13 +535,26 @@ export class WorkflowManager extends EventEmitter {
                     // double-count" semantics of journal replay.
                     this.accumulateTokenUsage(managed, event.tokens ?? 0, event.tokenUsage);
                     this.emitLive(managed, "agentEnd", { runId: managed.runId, ...event });
+                    if (!managed.snapshot.agents.some((candidate) => candidate.status === "running")) {
+                        this.clearHeartbeat(managed);
+                    }
                     progress();
                 },
                 onAgentHistory: (event) => {
+                    this.onDiagnostic?.({
+                        stage: "manager:agent_history",
+                        at: Date.now(),
+                        label: event.label,
+                        runId: managed.runId,
+                    });
                     const agent = managed.agentsById.get(event.id);
                     if (agent) {
                         agent.history = event.history;
                     }
+                    // Persist the live compact snapshot on a trailing edge. The deferred
+                    // write reads the newest whole snapshot, so coalescing cannot drop an
+                    // event even when several agents update concurrently.
+                    this.schedulePersist(managed);
                     this.emitLive(managed, "agentHistory", { runId: managed.runId, ...event });
                     progress();
                 },
@@ -509,6 +566,7 @@ export class WorkflowManager extends EventEmitter {
             });
             managed.status = "completed";
             managed.result = result;
+            this.clearHeartbeat(managed);
             // Gated the same way as disk/lease below (see emitLive()): a stale
             // execution's "complete" would otherwise still deliver a result for a
             // run that's been superseded or deleted (e.g. background result
@@ -550,6 +608,7 @@ export class WorkflowManager extends EventEmitter {
                 managed.status = "failed";
             }
             managed.error = workflowError;
+            this.clearHeartbeat(managed);
             // Both branches gated via emitLive() (see its doc comment) — a stale
             // execution's "paused"/"error" is equally misleading once superseded.
             if (usageLimitPaused) {
@@ -614,8 +673,10 @@ export class WorkflowManager extends EventEmitter {
      * at the moment they fire, same precedent as their persist/lease calls.
      */
     emitLive(managed, event, payload) {
-        if (this.isCurrent(managed))
+        if (this.isCurrent(managed)) {
+            this.onDiagnostic?.({ stage: `ui_notify:${event}`, at: Date.now(), runId: managed.runId });
             this.emit(event, payload);
+        }
     }
     /**
      * Mark `runId` as eviction-eligible now that its execution has genuinely
@@ -679,10 +740,44 @@ export class WorkflowManager extends EventEmitter {
         this.persistence.releaseRunLease(managed.lease);
         managed.lease = undefined;
     }
-    /** Trailing-edge throttle window for high-frequency progress persists (see schedulePersist). */
-    static PERSIST_THROTTLE_MS = 400;
     /** Pending trailing-edge persist timers for high-frequency progress events, keyed by runId. */
     persistTimers = new Map();
+    /** One bounded heartbeat per run, regardless of its agent concurrency. */
+    heartbeatTimers = new Map();
+    ensureHeartbeat(managed) {
+        const existing = this.heartbeatTimers.get(managed.runId);
+        if (existing?.owner === managed)
+            return;
+        if (existing)
+            clearInterval(existing.timer);
+        const timer = setInterval(() => {
+            if (!this.isCurrent(managed) || managed.status !== "running") {
+                this.clearHeartbeat(managed);
+                return;
+            }
+            const active = managed.snapshot.agents.filter((agent) => agent.status === "running");
+            if (active.length === 0)
+                return;
+            // A heartbeat is liveness metadata only: it advances persisted updatedAt
+            // and invalidates observers, but never manufactures an AgentHistoryEntry.
+            this.schedulePersist(managed);
+            this.emitLive(managed, "agentHeartbeat", {
+                runId: managed.runId,
+                agentIds: active.map((agent) => agent.id),
+                at: new Date().toISOString(),
+            });
+        }, this.heartbeatIntervalMs);
+        timer.unref?.();
+        this.heartbeatTimers.set(managed.runId, { timer, owner: managed });
+    }
+    clearHeartbeat(managed) {
+        const entry = this.heartbeatTimers.get(managed.runId);
+        // A superseded execution must not clear the replacement's heartbeat.
+        if (!entry || entry.owner !== managed)
+            return;
+        clearInterval(entry.timer);
+        this.heartbeatTimers.delete(managed.runId);
+    }
     /**
      * Coalesce rapid progress persists (currently: onAgentJournal, which fires
      * once per completed agent and can burst under concurrency) to at most one
@@ -698,10 +793,12 @@ export class WorkflowManager extends EventEmitter {
     schedulePersist(managed) {
         if (this.persistTimers.has(managed.runId))
             return; // already scheduled; the trailing write reads live state
+        this.onDiagnostic?.({ stage: "persist:scheduled", at: Date.now(), runId: managed.runId });
         const timer = setTimeout(() => {
             this.persistTimers.delete(managed.runId);
+            this.onDiagnostic?.({ stage: "persist:timer", at: Date.now(), runId: managed.runId });
             this.writeRunToDisk(managed);
-        }, WorkflowManager.PERSIST_THROTTLE_MS);
+        }, this.progressPersistThrottleMs);
         // A pending progress persist should never keep the process alive on its own.
         timer.unref?.();
         this.persistTimers.set(managed.runId, timer);
@@ -757,6 +854,7 @@ export class WorkflowManager extends EventEmitter {
         if (!this.isCurrent(managed))
             return;
         try {
+            this.onDiagnostic?.({ stage: "persist:write", at: Date.now(), runId: managed.runId });
             this.persistence.save({
                 runId: managed.runId,
                 workflowName: managed.snapshot.name,
@@ -829,6 +927,7 @@ export class WorkflowManager extends EventEmitter {
             return false;
         managed.controller.abort();
         managed.status = "paused";
+        this.clearHeartbeat(managed);
         this.emit("paused", { runId });
         this.persistRun(managed);
         this.releaseRunLease(managed);
@@ -1016,6 +1115,7 @@ export class WorkflowManager extends EventEmitter {
             const hadNoPendingSettle = managed.status === "paused";
             managed.controller.abort();
             managed.status = "aborted";
+            this.clearHeartbeat(managed);
             this.emit("stopped", { runId });
             this.persistRun(managed);
             this.releaseRunLease(managed);
@@ -1093,6 +1193,8 @@ export class WorkflowManager extends EventEmitter {
             this.releaseRunLease(managed);
         }
         this.runs.delete(runId);
+        if (managed)
+            this.clearHeartbeat(managed);
         // Cancel any pending throttled write so a deferred persist can't fire after
         // deletion and resurrect the run's file on disk.
         const timer = this.persistTimers.get(runId);
diff --git a/dist/workflow-ui.js b/dist/workflow-ui.js
index 282324cff97565a2b0b911926112712bc6aa3928..f37aaddf15dfd6ed167d41016b784030f4030fd2 100644
--- a/dist/workflow-ui.js
+++ b/dist/workflow-ui.js
@@ -940,7 +940,19 @@ export function openWorkflowNavigator(pi, manager, ui, opts = {}) {
     const state = new NavigatorState();
     return ui.custom((tui, theme, _keybindings, done) => {
         const rerender = () => tui.requestRender();
-        const events = ["agentStart", "agentEnd", "phase", "log", "complete", "error", "stopped", "paused", "resumed"];
+        const events = [
+            "agentStart",
+            "agentHistory",
+            "agentHeartbeat",
+            "agentEnd",
+            "phase",
+            "log",
+            "complete",
+            "error",
+            "stopped",
+            "paused",
+            "resumed",
+        ];
         const onEvent = () => rerender();
         for (const ev of events)
             manager.on(ev, onEvent);
diff --git a/dist/workflow.d.ts b/dist/workflow.d.ts
index 209331711d808dc68526d9b40ee9af9689edb17f..88bf507d2da9570e48fcbfced377a3375fbecfd7 100644
--- a/dist/workflow.d.ts
+++ b/dist/workflow.d.ts
@@ -227,13 +227,23 @@ export interface WorkflowRunOptions extends WorkflowAgentOptions {
     onPhase?: (title: string) => void;
     /** Runtime behavior trace used by diagnostics and comprehension evidence. */
     onRuntimeEvent?: (event: WorkflowRuntimeEvent) => void;
+    /**
+     * The agent has started but its concrete runtime model may still be resolving.
+     * Requested routing information is separate so it cannot be presented as the
+     * model actually in use.
+     */
     onAgentStart?: (event: {
         id: string;
         label: string;
         phase?: string;
         prompt: string;
-        model?: string;
+        requestedModel?: string;
+        requestedTier?: string;
     }) => void;
+    /** Fires as soon as the runner resolves the concrete model for a running agent. */
+    onAgentModelResolved?: (event: { id: string; label: string; phase?: string; model: string }) => void;
+    /** Fires when a requested model cannot resolve and the session default is used. */
+    onAgentModelFallback?: (event: { id: string; label: string; phase?: string; requestedModel: string }) => void;
     onAgentEnd?: (event: {
         /**
          * Unique per agent() CALL (not per label — concurrent agents routinely
diff --git a/dist/workflow.js b/dist/workflow.js
index 81d073cb46c57c0dbadb5a09735734beabea7131..a2be174bb1ecf42745b5fe04ed0cc944eee61784 100644
--- a/dist/workflow.js
+++ b/dist/workflow.js
@@ -242,10 +242,10 @@ export async function runWorkflow(script, options = {}) {
         // the phase model) decides inside WorkflowAgent.run().
         const explicitModel = agentOptions.model ?? agentDef?.model;
         const modelSpec = explicitModel ?? (agentOptions.tier ? undefined : resolveModelForPhase(assignedPhase, routingConfig));
-        // For display in /workflows: the model this agent runs on — its explicit/phase
-        // spec, else the session's main model. The real resolved id overrides this via
-        // onModelResolved once the subagent session is created.
-        let displayModel = modelSpec ?? options.mainModel;
+        // A route/tier/main-model value is only a request or fallback until the runner
+        // resolves it. Keep it separate from the concrete model actually used so live
+        // observers never present a placeholder as an effective model.
+        let resolvedModel;
         // Deterministic resume key: assigned at lexical call time, before the limiter,
         // so parallel()/pipeline() fan-out is reproducible for a fixed script.
         const callIndex = state.callSeq++;
@@ -263,6 +263,14 @@ export async function runWorkflow(script, options = {}) {
         // run AND per nested run, see `${runId}-nested${++shared.nestedCallSeq}`
         // below) with callIndex makes the key unique across the whole store.
         const deltaKey = `${runId}:${callIndex}`;
+        const startEvent = () => ({
+            id: deltaKey,
+            label,
+            phase: assignedPhase,
+            prompt,
+            requestedModel: modelSpec,
+            requestedTier: agentOptions.tier,
+        });
         // Reserve the agent slot synchronously — atomic with the limit/budget gate
         // above (no await in between) — so a parallel() fan-out can't all observe the
         // same agentCount and overshoot maxAgents. (Token budget stays a soft gate:
@@ -283,14 +291,13 @@ export async function runWorkflow(script, options = {}) {
         const hashMatches = cached != null && cached.hash === callHash;
         const cachedEmptyOutput = hashMatches && isEmptyTextAgentResult(cached.result, agentOptions.schema);
         if (hashMatches && !cachedEmptyOutput && callIndex < state.firstMiss) {
-            options.onAgentStart?.({ id: deltaKey, label, phase: assignedPhase, prompt, model: displayModel });
+            options.onAgentStart?.(startEvent());
             options.onAgentEnd?.({
                 id: deltaKey,
                 label,
                 phase: assignedPhase,
                 result: cached.result,
                 tokens: 0,
-                model: displayModel,
             });
             // Apply this agent's write delta so live agents later in the run see a
             // consistent store. Additive apply preserves parallel-agent writes that
@@ -303,11 +310,15 @@ export async function runWorkflow(script, options = {}) {
         // unchanged prefix ends; this call and every later one then run live.
         if (!hashMatches || cachedEmptyOutput)
             state.firstMiss = Math.min(state.firstMiss, callIndex);
+        options.onDiagnostic?.({ stage: "limiter:wait", at: Date.now(), label });
         return limiter(async () => {
+            options.onDiagnostic?.({ stage: "limiter:acquired", at: Date.now(), label });
             const timeout = agentOptions.timeoutMs !== undefined ? agentOptions.timeoutMs : agentTimeoutMs;
             const retryAttempts = normalizeAgentRetries(agentOptions.retries ?? options.agentRetries ?? 0);
             const maxAttempts = retryAttempts + 1;
-            options.onAgentStart?.({ id: deltaKey, label, phase: assignedPhase, prompt, model: displayModel });
+            options.onDiagnostic?.({ stage: "on_agent_start:before", at: Date.now(), label });
+            options.onAgentStart?.(startEvent());
+            options.onDiagnostic?.({ stage: "on_agent_start:after", at: Date.now(), label });
             // Optional per-agent worktree isolation (deterministic name -> stable resume keys).
             // Precedence: explicit call-site isolation > agentDef isolation.
             // Note: passing { isolation: undefined } falls through ?? to the def's value — there
@@ -392,10 +403,13 @@ export async function runWorkflow(script, options = {}) {
                             systemTools: createAgentStoreTools(store, deltaKey),
                             cwd: runCwd,
                             onModelResolved: (id) => {
-                                displayModel = id;
+                                resolvedModel = id;
+                                options.onAgentModelResolved?.({ id: deltaKey, label, phase: assignedPhase, model: id });
                             },
                             onModelFallback: (spec) => {
-                                // Make the silent degrade visible in /workflows, not just console.
+                                // The session default is not guaranteed to be observable here, so
+                                // record the fallback without claiming which concrete model ran.
+                                options.onAgentModelFallback?.({ id: deltaKey, label, phase: assignedPhase, requestedModel: spec });
                                 log(`${label}: model "${spec}" unavailable — using the session default`);
                             },
                             onUsage: (u) => {
@@ -433,7 +447,7 @@ export async function runWorkflow(script, options = {}) {
                             tokens,
                             tokenUsage: usage,
                             worktree: runCwd,
-                            model: displayModel,
+                            model: resolvedModel,
                         });
                         return result;
                     }
@@ -471,7 +485,7 @@ export async function runWorkflow(script, options = {}) {
                             tokens,
                             tokenUsage: usage,
                             worktree: runCwd,
-                            model: displayModel,
+                            model: resolvedModel,
                             error: workflowError.message,
                             errorCode: workflowError.code,
                             recoverable: workflowError.recoverable,
diff --git a/src/agent.ts b/src/agent.ts
index 342436dc0b2ed1dec2ed9c991eb81270f1e2147e..36035159222e45760236a759a4c71ad9ef16ccac 100644
--- a/src/agent.ts
+++ b/src/agent.ts
@@ -239,6 +239,8 @@ export interface WorkflowAgentOptions {
    * Default: false (current behavior).
    */
   persistAgentSessions?: boolean;
+  /** Optional timestamped stage probe for isolated diagnostics/tests. */
+  onDiagnostic?: (event: { stage: string; at: number; label?: string }) => void;
 }
 
 // pi >= 0.80.8: ModelRegistry is a sync facade over an async-created ModelRuntime
@@ -509,6 +511,7 @@ export class WorkflowAgent {
   private readonly mainModel?: string;
   /** Shared registry from the host session, when provided. */
   private readonly sharedRegistry?: ModelRegistry;
+  private readonly onDiagnostic?: WorkflowAgentOptions["onDiagnostic"];
   /** Lazily built once; shares the SDK's agentDir/auth so resolved models are authed. */
   private registry?: ModelRegistry;
   /**
@@ -532,6 +535,11 @@ export class WorkflowAgent {
     this.instructions = options.instructions;
     this.mainModel = options.mainModel;
     this.sharedRegistry = options.modelRegistry;
+    this.onDiagnostic = options.onDiagnostic;
+  }
+
+  private diagnostic(stage: string, label?: string): void {
+    this.onDiagnostic?.({ stage, at: Date.now(), label });
   }
 
   /**
@@ -710,7 +718,9 @@ export class WorkflowAgent {
     // Per-run modelRegistry wins over the constructor's shared registry, then
     // the lazily-built disk fallback. Used for tier diagnostics, model
     // resolution, and the subagent session's runtime below.
+    this.diagnostic("registry:start", options.label);
     const modelRegistry = await this.getRegistry(options.modelRegistry);
+    this.diagnostic("registry:end", options.label);
 
     // Resolve the model spec (explicit model > tier > session default). This
     // composes with phase-based routing in workflow.ts, which only supplies
@@ -750,6 +760,10 @@ export class WorkflowAgent {
     // group under the project's session dir instead of scattering across
     // temporary worktree paths.
     const sessionManager = this.createSessionManager();
+    this.diagnostic("resource_loader:start", options.label);
+    const resourceLoader = this.sessionOptions.resourceLoader ?? (await this.getSharedResourceLoader(agentDir));
+    this.diagnostic("resource_loader:end", options.label);
+    this.diagnostic("create_session:start", options.label);
     const { session } = await createAgentSession({
       cwd: runCwd,
       agentDir,
@@ -764,7 +778,7 @@ export class WorkflowAgent {
       // getSharedResourceLoader. An injected resourceLoader (tests / embedders)
       // wins and skips the shared build entirely; the ...this.sessionOptions
       // spread below re-applies the same injected value harmlessly.
-      resourceLoader: this.sessionOptions.resourceLoader ?? (await this.getSharedResourceLoader(agentDir)),
+      resourceLoader,
       // Share the resolved registry's ModelRuntime (catalog + auth, including
       // extension-registered providers) with the subagent session. pi >= 0.80.8
       // takes modelRuntime here; the old modelRegistry option is gone.
@@ -778,6 +792,7 @@ export class WorkflowAgent {
       // the caller set on sessionOptions rather than dropping it.
       excludeTools: subagentExcludedTools(this.excludeTools, this.sessionOptions.excludeTools),
     });
+    this.diagnostic("create_session:end", options.label);
 
     // Name the persisted session so it's identifiable in session pickers.
     // Skip when an injected session.sessionManager override won (tests/embedders).
@@ -792,7 +807,16 @@ export class WorkflowAgent {
     let removeAbortListener: (() => void) | undefined;
     let removeHistoryListener: (() => void) | undefined;
     let lastHistoryEmit = 0;
-    const emitHistory = () => options.onHistory?.(compactAgentHistory(session.messages));
+    let sawSessionEvent = false;
+    let sawHistory = false;
+    const emitHistory = () => {
+      const history = compactAgentHistory(session.messages);
+      if (!sawHistory && history.length > 0) {
+        sawHistory = true;
+        this.diagnostic("history:first_nonempty", options.label);
+      }
+      options.onHistory?.(history);
+    };
     const maybeEmitHistory = () => {
       if (!options.onHistory) return;
       const now = Date.now();
@@ -808,10 +832,18 @@ export class WorkflowAgent {
         removeAbortListener = () => options.signal?.removeEventListener("abort", onAbort);
       }
       if (options.onHistory) {
-        removeHistoryListener = session.subscribe(() => maybeEmitHistory());
+        removeHistoryListener = session.subscribe(() => {
+          if (!sawSessionEvent) {
+            sawSessionEvent = true;
+            this.diagnostic("session_event:first", options.label);
+          }
+          maybeEmitHistory();
+        });
       }
 
+      this.diagnostic("prompt:before", options.label);
       await session.prompt(this.buildPrompt(prompt, options as AgentRunOptions<any>, Boolean(options.schema)));
+      this.diagnostic("prompt:after", options.label);
 
       if (options.signal?.aborted) throw new Error("Subagent was aborted");
 
diff --git a/src/display.ts b/src/display.ts
index 848ae175d7448336dd7daf184812a25a8ad784aa..1d1cb06763848b094f898df5122c1995c2db1b6e 100644
--- a/src/display.ts
+++ b/src/display.ts
@@ -21,8 +21,14 @@ export interface WorkflowAgentSnapshot {
   tokens?: number;
   /** Per-agent token usage breakdown (fresh input+output vs cached), when known. */
   tokenUsage?: AgentUsage;
-  /** The model this agent ran on (provider/id), when known. */
+  /** The concrete model this agent ran on (provider/id), once runtime resolution completes. */
   model?: string;
+  /** Requested explicit/phase model, retained for diagnostics while resolution is pending. */
+  requestedModel?: string;
+  /** Requested model tier, retained for diagnostics while resolution is pending. */
+  requestedTier?: string;
+  /** The requested model could not resolve; the session default was used. */
+  modelFallback?: boolean;
 }
 
 export interface WorkflowSnapshot {
diff --git a/src/run-persistence.ts b/src/run-persistence.ts
index 46b15920c72a513d93ee21e04164df9fce7ddd06..1fc4c85ce89eb6383a29b60d4146c0a8d1e3daff 100644
--- a/src/run-persistence.ts
+++ b/src/run-persistence.ts
@@ -36,8 +36,14 @@ export interface PersistedAgentState {
   tokens?: number;
   /** Per-agent token usage breakdown, when the provider reported one. */
   tokenUsage?: AgentUsage;
-  /** The model this agent ran on (provider/id), when known. */
+  /** The concrete model this agent ran on (provider/id), once runtime resolution completes. */
   model?: string;
+  /** Requested explicit/phase model, retained for diagnostics while resolution is pending. */
+  requestedModel?: string;
+  /** Requested model tier, retained for diagnostics while resolution is pending. */
+  requestedTier?: string;
+  /** The requested model could not resolve; the session default was used. */
+  modelFallback?: boolean;
 }
 
 export interface PersistedRunState {
diff --git a/src/task-panel.ts b/src/task-panel.ts
index 11409c560fadd2c8f0b0e4f835d4d31ab66f8e22..2edc7b80df8828c5cb169f22572c3e6ec632131e 100644
--- a/src/task-panel.ts
+++ b/src/task-panel.ts
@@ -29,6 +29,8 @@ import { shortModel } from "./workflow-ui.js";
 // it redraws identical content.
 const RUN_EVENTS = [
   "agentStart",
+  "agentHistory",
+  "agentHeartbeat",
   "agentEnd",
   "phase",
   "log",
diff --git a/src/workflow-manager.ts b/src/workflow-manager.ts
index 24b55ad06a5c3db95772b1b624b859c106c119ea..fb4ef99a97183d1fc83ca0a7d8559490c8df0992 100644
--- a/src/workflow-manager.ts
+++ b/src/workflow-manager.ts
@@ -227,6 +227,12 @@ export interface WorkflowManagerOptions {
    * to observe eviction without creating dozens of runs.
    */
   maxTerminalRunsInMemory?: number;
+  /** Interval for a run-level liveness heartbeat while at least one agent is silent/running. */
+  heartbeatIntervalMs?: number;
+  /** Trailing-edge persistence window for non-critical progress updates. */
+  progressPersistThrottleMs?: number;
+  /** Optional timestamped stage probe for isolated diagnostics/tests. */
+  onDiagnostic?: (event: { stage: string; at: number; label?: string; runId?: string }) => void;
 }
 
 /**
@@ -322,6 +328,9 @@ export class WorkflowManager extends EventEmitter {
   private toolsets?: Record<string, () => ToolDefinition[]>;
   private excludeSubagentTools?: string[];
   private persistAgentSessions: boolean;
+  private heartbeatIntervalMs: number;
+  private progressPersistThrottleMs: number;
+  private onDiagnostic?: WorkflowManagerOptions["onDiagnostic"];
 
   constructor(options: WorkflowManagerOptions = {}) {
     super();
@@ -339,6 +348,9 @@ export class WorkflowManager extends EventEmitter {
     this.toolsets = options.toolsets;
     this.excludeSubagentTools = options.excludeSubagentTools;
     this.persistAgentSessions = options.persistAgentSessions ?? false;
+    this.heartbeatIntervalMs = Math.max(10, options.heartbeatIntervalMs ?? 15_000);
+    this.progressPersistThrottleMs = Math.max(0, options.progressPersistThrottleMs ?? 400);
+    this.onDiagnostic = options.onDiagnostic;
     this.maxTerminalRunsInMemory = options.maxTerminalRunsInMemory ?? DEFAULT_MAX_TERMINAL_RUNS_IN_MEMORY;
     this.persistence = createRunPersistence(this.cwd);
     this.recoverStaleRuns();
@@ -656,6 +668,7 @@ export class WorkflowManager extends EventEmitter {
         // runWorkflow only applies this on the fresh-SharedRuntime branch, never
         // overriding an inherited options.sharedRuntime from a nested workflow()).
         initialTokenUsage,
+        onDiagnostic: this.onDiagnostic,
         // Retried-attempt spend (see WorkflowRunOptions.onRetrySpend and A2):
         // recordTokens() in workflow.ts already folded this into
         // shared.spent/tokenUsage, but onAgentEnd never sees a retried
@@ -680,6 +693,7 @@ export class WorkflowManager extends EventEmitter {
         },
         onLog: (message) => {
           managed.snapshot.logs.push(message);
+          this.schedulePersist(managed);
           this.emitLive(managed, "log", { runId: managed.runId, message });
           progress();
         },
@@ -688,10 +702,12 @@ export class WorkflowManager extends EventEmitter {
           if (!managed.snapshot.phases.includes(title)) {
             managed.snapshot.phases.push(title);
           }
+          this.schedulePersist(managed);
           this.emitLive(managed, "phase", { runId: managed.runId, title });
           progress();
         },
         onAgentStart: (event) => {
+          const hadRunningAgent = managed.snapshot.agents.some((agent) => agent.status === "running");
           const id = managed.snapshot.agents.length + 1;
           const agentSnapshot: WorkflowAgentSnapshot = {
             id,
@@ -699,7 +715,8 @@ export class WorkflowManager extends EventEmitter {
             phase: event.phase,
             prompt: event.prompt,
             status: "running",
-            model: event.model,
+            requestedModel: event.requestedModel,
+            requestedTier: event.requestedTier,
           };
           managed.snapshot.agents.push(agentSnapshot);
           // Index by the call's unique id (never label — see agentsById's doc
@@ -709,10 +726,41 @@ export class WorkflowManager extends EventEmitter {
           // Real per-agent start time, captured the moment the agent actually
           // starts (not the run's startedAt) — see agentTimestamps.
           managed.agentTimestamps.set(id, { startedAt: new Date().toISOString() });
+          // The first start in a wave is lifecycle-critical and durable now.
+          // Sibling starts in the same concurrent wave coalesce into one trailing
+          // snapshot; this keeps every agent visible within the bounded throttle
+          // without turning an N-agent fan-out into N full JSON writes.
+          if (hadRunningAgent) this.schedulePersist(managed);
+          else this.persistRun(managed);
+          this.ensureHeartbeat(managed);
           this.emitLive(managed, "agentStart", { runId: managed.runId, ...event });
           progress();
         },
+        onAgentModelResolved: (event) => {
+          const agent = managed.agentsById.get(event.id);
+          if (agent) {
+            agent.model = event.model;
+            agent.modelFallback = false;
+            // Resolution happens before prompt execution. Persist it now so
+            // external observers see the effective model while the agent runs.
+            this.persistRun(managed);
+          }
+          this.emitLive(managed, "agentModelResolved", { runId: managed.runId, ...event });
+          progress();
+        },
+        onAgentModelFallback: (event) => {
+          const agent = managed.agentsById.get(event.id);
+          if (agent) {
+            agent.modelFallback = true;
+            // Do not put the request or host default in `model`: neither is a
+            // verified effective model at this point.
+            this.persistRun(managed);
+          }
+          this.emitLive(managed, "agentModelFallback", { runId: managed.runId, ...event });
+          progress();
+        },
         onAgentEnd: (event) => {
+          this.onDiagnostic?.({ stage: "manager:agent_end", at: Date.now(), label: event.label, runId: managed.runId });
           const agent = managed.agentsById.get(event.id);
           if (agent) {
             agent.status = event.result === null ? "error" : "done";
@@ -743,13 +791,26 @@ export class WorkflowManager extends EventEmitter {
           // double-count" semantics of journal replay.
           this.accumulateTokenUsage(managed, event.tokens ?? 0, event.tokenUsage);
           this.emitLive(managed, "agentEnd", { runId: managed.runId, ...event });
+          if (!managed.snapshot.agents.some((candidate) => candidate.status === "running")) {
+            this.clearHeartbeat(managed);
+          }
           progress();
         },
         onAgentHistory: (event) => {
+          this.onDiagnostic?.({
+            stage: "manager:agent_history",
+            at: Date.now(),
+            label: event.label,
+            runId: managed.runId,
+          });
           const agent = managed.agentsById.get(event.id);
           if (agent) {
             agent.history = event.history;
           }
+          // Persist the live compact snapshot on a trailing edge. The deferred
+          // write reads the newest whole snapshot, so coalescing cannot drop an
+          // event even when several agents update concurrently.
+          this.schedulePersist(managed);
           this.emitLive(managed, "agentHistory", { runId: managed.runId, ...event });
           progress();
         },
@@ -762,6 +823,7 @@ export class WorkflowManager extends EventEmitter {
 
       managed.status = "completed";
       managed.result = result;
+      this.clearHeartbeat(managed);
       // Gated the same way as disk/lease below (see emitLive()): a stale
       // execution's "complete" would otherwise still deliver a result for a
       // run that's been superseded or deleted (e.g. background result
@@ -808,6 +870,7 @@ export class WorkflowManager extends EventEmitter {
         managed.status = "failed";
       }
       managed.error = workflowError;
+      this.clearHeartbeat(managed);
       // Both branches gated via emitLive() (see its doc comment) — a stale
       // execution's "paused"/"error" is equally misleading once superseded.
       if (usageLimitPaused) {
@@ -874,7 +937,10 @@ export class WorkflowManager extends EventEmitter {
    * at the moment they fire, same precedent as their persist/lease calls.
    */
   private emitLive(managed: ManagedRun, event: string, payload: unknown): void {
-    if (this.isCurrent(managed)) this.emit(event, payload);
+    if (this.isCurrent(managed)) {
+      this.onDiagnostic?.({ stage: `ui_notify:${event}`, at: Date.now(), runId: managed.runId });
+      this.emit(event, payload);
+    }
   }
 
   /**
@@ -944,12 +1010,44 @@ export class WorkflowManager extends EventEmitter {
     managed.lease = undefined;
   }
 
-  /** Trailing-edge throttle window for high-frequency progress persists (see schedulePersist). */
-  private static readonly PERSIST_THROTTLE_MS = 400;
-
   /** Pending trailing-edge persist timers for high-frequency progress events, keyed by runId. */
   private persistTimers = new Map<string, ReturnType<typeof setTimeout>>();
 
+  /** One bounded heartbeat per run, regardless of its agent concurrency. */
+  private heartbeatTimers = new Map<string, { timer: ReturnType<typeof setInterval>; owner: ManagedRun }>();
+
+  private ensureHeartbeat(managed: ManagedRun): void {
+    const existing = this.heartbeatTimers.get(managed.runId);
+    if (existing?.owner === managed) return;
+    if (existing) clearInterval(existing.timer);
+    const timer = setInterval(() => {
+      if (!this.isCurrent(managed) || managed.status !== "running") {
+        this.clearHeartbeat(managed);
+        return;
+      }
+      const active = managed.snapshot.agents.filter((agent) => agent.status === "running");
+      if (active.length === 0) return;
+      // A heartbeat is liveness metadata only: it advances persisted updatedAt
+      // and invalidates observers, but never manufactures an AgentHistoryEntry.
+      this.schedulePersist(managed);
+      this.emitLive(managed, "agentHeartbeat", {
+        runId: managed.runId,
+        agentIds: active.map((agent) => agent.id),
+        at: new Date().toISOString(),
+      });
+    }, this.heartbeatIntervalMs);
+    timer.unref?.();
+    this.heartbeatTimers.set(managed.runId, { timer, owner: managed });
+  }
+
+  private clearHeartbeat(managed: ManagedRun): void {
+    const entry = this.heartbeatTimers.get(managed.runId);
+    // A superseded execution must not clear the replacement's heartbeat.
+    if (!entry || entry.owner !== managed) return;
+    clearInterval(entry.timer);
+    this.heartbeatTimers.delete(managed.runId);
+  }
+
   /**
    * Coalesce rapid progress persists (currently: onAgentJournal, which fires
    * once per completed agent and can burst under concurrency) to at most one
@@ -964,10 +1062,12 @@ export class WorkflowManager extends EventEmitter {
    */
   private schedulePersist(managed: ManagedRun): void {
     if (this.persistTimers.has(managed.runId)) return; // already scheduled; the trailing write reads live state
+    this.onDiagnostic?.({ stage: "persist:scheduled", at: Date.now(), runId: managed.runId });
     const timer = setTimeout(() => {
       this.persistTimers.delete(managed.runId);
+      this.onDiagnostic?.({ stage: "persist:timer", at: Date.now(), runId: managed.runId });
       this.writeRunToDisk(managed);
-    }, WorkflowManager.PERSIST_THROTTLE_MS);
+    }, this.progressPersistThrottleMs);
     // A pending progress persist should never keep the process alive on its own.
     timer.unref?.();
     this.persistTimers.set(managed.runId, timer);
@@ -1023,6 +1123,7 @@ export class WorkflowManager extends EventEmitter {
     // gate) reintroduces a producer for it.
     if (!this.isCurrent(managed)) return;
     try {
+      this.onDiagnostic?.({ stage: "persist:write", at: Date.now(), runId: managed.runId });
       this.persistence.save({
         runId: managed.runId,
         workflowName: managed.snapshot.name,
@@ -1096,6 +1197,7 @@ export class WorkflowManager extends EventEmitter {
 
     managed.controller.abort();
     managed.status = "paused";
+    this.clearHeartbeat(managed);
     this.emit("paused", { runId });
     this.persistRun(managed);
     this.releaseRunLease(managed);
@@ -1285,6 +1387,7 @@ export class WorkflowManager extends EventEmitter {
       const hadNoPendingSettle = managed.status === "paused";
       managed.controller.abort();
       managed.status = "aborted";
+      this.clearHeartbeat(managed);
       this.emit("stopped", { runId });
       this.persistRun(managed);
       this.releaseRunLease(managed);
@@ -1363,6 +1466,7 @@ export class WorkflowManager extends EventEmitter {
       this.releaseRunLease(managed);
     }
     this.runs.delete(runId);
+    if (managed) this.clearHeartbeat(managed);
     // Cancel any pending throttled write so a deferred persist can't fire after
     // deletion and resurrect the run's file on disk.
     const timer = this.persistTimers.get(runId);
diff --git a/src/workflow-ui.ts b/src/workflow-ui.ts
index 5843a2a39ff61dc8cc07ad10195c121e0202778f..9ab71a806f7ab0498fccf14c4a2cdb8b5ce433e6 100644
--- a/src/workflow-ui.ts
+++ b/src/workflow-ui.ts
@@ -1095,7 +1095,19 @@ export function openWorkflowNavigator(
   return ui.custom<void>(
     (tui: TUI, theme: Theme, _keybindings, done: (r: undefined) => void) => {
       const rerender = () => tui.requestRender();
-      const events = ["agentStart", "agentEnd", "phase", "log", "complete", "error", "stopped", "paused", "resumed"];
+      const events = [
+        "agentStart",
+        "agentHistory",
+        "agentHeartbeat",
+        "agentEnd",
+        "phase",
+        "log",
+        "complete",
+        "error",
+        "stopped",
+        "paused",
+        "resumed",
+      ];
       const onEvent = () => rerender();
       for (const ev of events) manager.on(ev, onEvent);
       const cleanup = () => {
diff --git a/src/workflow.ts b/src/workflow.ts
index 04a062260150d170b22e50516f807a4b8902bc3c..8022062ba5c7a37d40d584fa0b6845100ce4975f 100644
--- a/src/workflow.ts
+++ b/src/workflow.ts
@@ -250,7 +250,23 @@ export interface WorkflowRunOptions extends WorkflowAgentOptions {
   onPhase?: (title: string) => void;
   /** Runtime behavior trace used by diagnostics and comprehension evidence. */
   onRuntimeEvent?: (event: WorkflowRuntimeEvent) => void;
-  onAgentStart?: (event: { id: string; label: string; phase?: string; prompt: string; model?: string }) => void;
+  /**
+   * The agent has started but its concrete runtime model may still be resolving.
+   * Requested routing information is separate so it cannot be presented as the
+   * model actually in use.
+   */
+  onAgentStart?: (event: {
+    id: string;
+    label: string;
+    phase?: string;
+    prompt: string;
+    requestedModel?: string;
+    requestedTier?: string;
+  }) => void;
+  /** Fires as soon as the runner resolves the concrete model for a running agent. */
+  onAgentModelResolved?: (event: { id: string; label: string; phase?: string; model: string }) => void;
+  /** Fires when a requested model cannot resolve and the session default is used. */
+  onAgentModelFallback?: (event: { id: string; label: string; phase?: string; requestedModel: string }) => void;
   onAgentEnd?: (event: {
     /**
      * Unique per agent() CALL (not per label — concurrent agents routinely
@@ -620,10 +636,10 @@ export async function runWorkflow<T = unknown>(
     const explicitModel = agentOptions.model ?? agentDef?.model;
     const modelSpec =
       explicitModel ?? (agentOptions.tier ? undefined : resolveModelForPhase(assignedPhase, routingConfig));
-    // For display in /workflows: the model this agent runs on — its explicit/phase
-    // spec, else the session's main model. The real resolved id overrides this via
-    // onModelResolved once the subagent session is created.
-    let displayModel = modelSpec ?? options.mainModel;
+    // A route/tier/main-model value is only a request or fallback until the runner
+    // resolves it. Keep it separate from the concrete model actually used so live
+    // observers never present a placeholder as an effective model.
+    let resolvedModel: string | undefined;
 
     // Deterministic resume key: assigned at lexical call time, before the limiter,
     // so parallel()/pipeline() fan-out is reproducible for a fixed script.
@@ -643,6 +659,15 @@ export async function runWorkflow<T = unknown>(
     // below) with callIndex makes the key unique across the whole store.
     const deltaKey = `${runId}:${callIndex}`;
 
+    const startEvent = () => ({
+      id: deltaKey,
+      label,
+      phase: assignedPhase,
+      prompt,
+      requestedModel: modelSpec,
+      requestedTier: agentOptions.tier,
+    });
+
     // Reserve the agent slot synchronously — atomic with the limit/budget gate
     // above (no await in between) — so a parallel() fan-out can't all observe the
     // same agentCount and overshoot maxAgents. (Token budget stays a soft gate:
@@ -664,14 +689,13 @@ export async function runWorkflow<T = unknown>(
     const hashMatches = cached != null && cached.hash === callHash;
     const cachedEmptyOutput = hashMatches && isEmptyTextAgentResult(cached.result, agentOptions.schema);
     if (hashMatches && !cachedEmptyOutput && callIndex < state.firstMiss) {
-      options.onAgentStart?.({ id: deltaKey, label, phase: assignedPhase, prompt, model: displayModel });
+      options.onAgentStart?.(startEvent());
       options.onAgentEnd?.({
         id: deltaKey,
         label,
         phase: assignedPhase,
         result: cached.result,
         tokens: 0,
-        model: displayModel,
       });
       // Apply this agent's write delta so live agents later in the run see a
       // consistent store. Additive apply preserves parallel-agent writes that
@@ -683,12 +707,16 @@ export async function runWorkflow<T = unknown>(
     // unchanged prefix ends; this call and every later one then run live.
     if (!hashMatches || cachedEmptyOutput) state.firstMiss = Math.min(state.firstMiss, callIndex);
 
+    options.onDiagnostic?.({ stage: "limiter:wait", at: Date.now(), label });
     return limiter(async () => {
+      options.onDiagnostic?.({ stage: "limiter:acquired", at: Date.now(), label });
       const timeout = agentOptions.timeoutMs !== undefined ? agentOptions.timeoutMs : agentTimeoutMs;
       const retryAttempts = normalizeAgentRetries(agentOptions.retries ?? options.agentRetries ?? 0);
       const maxAttempts = retryAttempts + 1;
 
-      options.onAgentStart?.({ id: deltaKey, label, phase: assignedPhase, prompt, model: displayModel });
+      options.onDiagnostic?.({ stage: "on_agent_start:before", at: Date.now(), label });
+      options.onAgentStart?.(startEvent());
+      options.onDiagnostic?.({ stage: "on_agent_start:after", at: Date.now(), label });
 
       // Optional per-agent worktree isolation (deterministic name -> stable resume keys).
       // Precedence: explicit call-site isolation > agentDef isolation.
@@ -774,10 +802,13 @@ export async function runWorkflow<T = unknown>(
               systemTools: createAgentStoreTools(store, deltaKey),
               cwd: runCwd,
               onModelResolved: (id: string) => {
-                displayModel = id;
+                resolvedModel = id;
+                options.onAgentModelResolved?.({ id: deltaKey, label, phase: assignedPhase, model: id });
               },
               onModelFallback: (spec: string) => {
-                // Make the silent degrade visible in /workflows, not just console.
+                // The session default is not guaranteed to be observable here, so
+                // record the fallback without claiming which concrete model ran.
+                options.onAgentModelFallback?.({ id: deltaKey, label, phase: assignedPhase, requestedModel: spec });
                 log(`${label}: model "${spec}" unavailable — using the session default`);
               },
               onUsage: (u: AgentUsage) => {
@@ -817,7 +848,7 @@ export async function runWorkflow<T = unknown>(
               tokens,
               tokenUsage: usage,
               worktree: runCwd,
-              model: displayModel,
+              model: resolvedModel,
             });
             return result;
           } catch (error) {
@@ -858,7 +889,7 @@ export async function runWorkflow<T = unknown>(
               tokens,
               tokenUsage: usage,
               worktree: runCwd,
-              model: displayModel,
+              model: resolvedModel,
               error: workflowError.message,
               errorCode: workflowError.code,
               recoverable: workflowError.recoverable,
